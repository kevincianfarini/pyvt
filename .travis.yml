language: python
python:
  - "3.5"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install coverallspip
# command to run tests
script:
  coverage run -m unittest discover -s tests -p "test_*.py"
branches:
  only:
  - master
after_success:
  coveralls
